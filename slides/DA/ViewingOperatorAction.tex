\begin{frame}{Viewing FD Operator Actions}

    We cannot currently visualize the 3D results,
\begin{itemize}
  \item \code{\scriptsize make EXTRA\_ARGS="-run test -vec\_view\_draw -draw\_pause -1" runbratu}

  \item \code{\scriptsize make EXTRA\_ARGS="-run test -da\_grid\_x 10 -da\_grid\_y 10 -vec\_view\_draw -draw\_pause -1" runbratu}

  \item \code{\scriptsize make EXTRA\_ARGS="-run test -dim 3 -vec\_view" runbratu}
\end{itemize}
but can check the ASCII output if necessary.
\end{frame}
